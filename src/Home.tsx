import React from 'react';
import CarroEverywhere from './CarroEverywhere';

const Home: React.FC = () => {
  return (
    <div
      style={{
        minWidth: '100vw',
        minHeight: '100vh',
      }}
    >
      <CarroEverywhere />
    </div>
  );
};

export default Home;
